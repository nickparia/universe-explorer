{
  "name": "solace",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "three": "0.170.0"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "vite": "^6.3.5",
    "wrangler": "^4.0.0"
  }
}
